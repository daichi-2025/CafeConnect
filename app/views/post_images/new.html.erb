<h1>投稿フォーム</h1>
<%= form_with model: @post_image do |f| %>
  <h4>ショップ名</h4>
  <%= f.text_field :shop_name %>
  <h4>投稿内容</h4>
  <%= f.text_area :caption %>
  <h4>画像</h4>
  <%= f.file_field :image, accept: "image/*" %>
  <%= f.submit '投稿' %>
<% end %>